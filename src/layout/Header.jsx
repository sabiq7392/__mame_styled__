import {  Header as MameHeader, H1, P, A, Div } from '../../styles/MameStyled/core/HtmlTag.styled';
import stylesConfig from '../../styles.config';
import Description from '../components/atoms/Description';

export default function Header() {
  const { color, spacing, radius, timing } = stylesConfig;

  return (
    <></>
    // <MameHeader css={{ position: 'sticky', top: 0, background: 'rgba(0, 30, 60, .7)', backdropFilter: '' }} debug>
    //   <Appbar />
    // </MameHeader>
  );
}
