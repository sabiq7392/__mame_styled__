export const valueLikeCss = (propName: string, cssName: string): string => `Value of ${propName} is same like ${cssName} in css`;
export const isSimplified = (propNameLikeCss: string) => `When its without ${propNameLikeCss} it just simplified`;
